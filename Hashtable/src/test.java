
if(lastFourLetters.charAt(0)=='.'){
        throw new OperationDeniedException(INVALID_INPUT);
        }
        lastFiveLetters = lowerCase.substring(lowerCase.length() - FOUR_LONG_EXTENSION_LENGTH);
        }
        else{